Write a program to change the given string to uppercase.
Sample Input 1

Java Language
Sample Output 1

JAVA LANGUAGE

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    char ch[20];
    gets(ch);
    for(int i=0;ch[i]!='\0';i++)
        if(ch[i]>=97 && ch[i]<=122)
            ch[i]=ch[i]-32;
    printf("%s",ch);
}
