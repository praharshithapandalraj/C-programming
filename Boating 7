#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int w,a,c;
    scanf("%d%d%d",&w,&a,&c);
    w=(a*75)+(c*30);
    if(w<=500){
        printf("Boat is stable");
    }else{
        printf("Boat will drown");
    }
    return 0;
}
