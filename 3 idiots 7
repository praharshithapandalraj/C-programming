#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x1,y1,x2,y2;
    scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
    float x=x1+x2;
    x=x/2;
    float y=y1+y2;
    y=y/2;
    printf("Binoy's house is located at (%.1f,%.1f)",x,y);
    return 0;
}
