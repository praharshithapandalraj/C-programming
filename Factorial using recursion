Factorial using recursion
input 5
output 120
code:
#include<stdio.h>
int fact(int m)
{
    if(m==0)
    return 1;
    fact(m-1);
    return m*fact(m-1);
}
void main()
{
    int n;
    scanf("%d",&n);
    printf("%d",fact(n));
}
