Print n natural numbers in reverse order using flag and with comma
input : 5
output : 5,4,3,2,1

#include <stdio.h>
void main()
{
    int i,n,flag;
    scanf("%d",&n);
    i=n;
    flag=0;
    while(i>=1){
        if(flag==0){
            printf("%d",i);
            flag=1;
        }
        else{
            printf(",%d",i);
            
        }i--;
    }
}
